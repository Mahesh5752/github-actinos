#this will deploy the safe / secure / tested app to prod server
 
name: Deploy to Server

on: 
     workflow_call:

jobs:
    Deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            
            - name: set DOCKER_TAG env variable
              run: |
                    echo "DOCKER_TAG=${{ github.event.workflow_run.head_sha }}" >> $GITHUB_ENV
            
            - name: SSH to pro server & create app folder
              uses: appleboy/ssh-action@v1.0.3
              with:
                 host: ${{ secrets.EC2_SSH_HOST }}
                 username: ${{ secrets.EC2_SSH_USER }}
                 key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}

                 script: |
                    echo "installing required packages & folders"
                    sudo apt-get update && sudo apt-get install docker.io docker-compose-v2 -y
                    sudo usermod -aG docker $USER && newgrp docker
                    mkdir -p devops
            
            

            - name: copy the docker-compose file
              uses: appleboy/scp-action@v1
              with:
                 host: ${{ secrets.EC2_SSH_HOST }}
                 username: ${{ secrets.EC2_SSH_USER }}
                 key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
                 source: "docker-compose.yml"
                 target: ~/devops
        
            - name: SSH to pro server & run the app
              uses: appleboy/ssh-action@v1.0.3
              with:
                 host: ${{ secrets.EC2_SSH_HOST }}
                 username: ${{ secrets.EC2_SSH_USER }}
                 key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}

                 script: |
                    echo "Running  App"
                    
                    export DOCKER_USER=${{ vars.DOCKERHUB_USER }}
                    export DOCKER_TAG=${{ github.event.workflow_run.head_sha }}
                    export HEAD_SHA=${{ env.DOCKER_TAG }}
                    echo ${{ secrets.DOCKERHUB_TOKEN }} | docker login --username ${{ vars.DOCKERHUB_USER }} --password-stdin    
                    cd ~/devops && docker compose down && docker compose up -d --build --force-recreate --pull always

                    Set correct variables
                      DOCKERHUB_USER=${{ secrets.DOCKERHUB_USER }}
                      DOCKER_TAG=latest

                      echo "User: $DOCKERHUB_USER"
                      echo "Tag: $DOCKER_TAG"

                      # Docker login (correct)
                      echo "${{ secrets.DOCKERHUB_TOKEN }}" | sudo docker login -u "${{ vars.DOCKERHUB_USER }}" --password-stdin

                      cd ~/devops

                      # Pass env inline (important)
                      sudo DOCKERHUB_USER=$DOCKERHUB_USER DOCKER_TAG=$DOCKER_TAG docker compose down
                      sudo DOCKERHUB_USER=$DOCKERHUB_USER DOCKER_TAG=$DOCKER_TAG docker compose up -d --pull always --force-recreate